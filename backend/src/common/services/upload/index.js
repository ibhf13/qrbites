const {
  upload,
  memoryUpload,
  writeMemoryFilesToDisk,
  cleanupFiles,
  getFileUrl,
  multerErrorHandler,
  dirs,
} = require('./fileUploadService')

module.exports = {
  upload,
  memoryUpload,
  writeMemoryFilesToDisk,
  cleanupFiles,
  getFileUrl,
  multerErrorHandler,
  dirs,
}

